apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-320
  name: sample-app-320
  namespace: ns-64
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-320
