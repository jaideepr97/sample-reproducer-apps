apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-79
  name: sample-app-79
  namespace: ns-16
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-79
