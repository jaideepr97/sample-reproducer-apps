apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-496
  name: sample-app-496
  namespace: ns-100
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-496
