apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-3
  name: sample-app-3
  namespace: ns-1
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-3
