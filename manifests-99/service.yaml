apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-99
  name: sample-app-99
  namespace: ns-20
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-99
