apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-450
  name: sample-app-450
  namespace: ns-90
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-450
