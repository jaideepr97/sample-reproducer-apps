apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-471
  name: sample-app-471
  namespace: ns-95
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-471
