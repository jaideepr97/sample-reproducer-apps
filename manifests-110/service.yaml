apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-110
  name: sample-app-110
  namespace: ns-22
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-110
