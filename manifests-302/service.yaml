apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-302
  name: sample-app-302
  namespace: ns-61
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-302
