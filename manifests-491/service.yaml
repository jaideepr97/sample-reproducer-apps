apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-491
  name: sample-app-491
  namespace: ns-99
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-491
