apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-371
  name: sample-app-371
  namespace: ns-75
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-371
