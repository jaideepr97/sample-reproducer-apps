apiVersion: v1
kind: Service
metadata:
  labels:
    app: sample-app-433
  name: sample-app-433
  namespace: ns-87
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: sample-app-433
