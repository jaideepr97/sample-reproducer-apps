apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: sample-app-1
  name: sample-app-1
  namespace: ns-1
spec:
  host: "PATCH_ME"
  port:
    targetPort: 8080
  to:
    kind: Service
    name: sample-app-1
    weight: 100
status:
  ingress:
  - conditions:
    - status: "True"
      type: Admitted
